//------------------------------------------------------------------------
// ** Accede a los contenidos de un arreglo utilizando la notacion de corchetes

// let ourVar = Array[0];

// Agrega
// push() => Agrega elemento al final del arreglo
// unshift() => Agrega elemento al inicio del arreglo

// Elimina
// pop() => Elimina al final del arreglo
// shift() => Elimina al inicio del arreglo
// splice(posicion inicial, posicion final) => Elimina cualquier numero de elementos consecutivos

// Eliminar y agregar
// splice(inicio, final, datos a agregar, d2) => Se puede eliminar n cantidad de datos y agregar m cantidad

// Copia o extrae
// slice(posicion de inicio, posicion fin + 1) La ultima posicion que se indica no es copiada
// Propagacion thatArray = [...thisArray];

// Combinacion de arreglos
/**
 * let thisArray = ['sage', 'rosemary', 'parsley', 'thyme'];
 * let thatArray = ['basil', 'cilantro', ...thisArray, 'coriander'];
 */

// Comprueba la existencia
/**
 * indexOf()
 * Existe       => Posicion
 * No existe    => -1
 * 
 */